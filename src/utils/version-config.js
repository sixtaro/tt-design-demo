/**
 * 版本配置文件
 * 集中管理所有组件和库的版本号
 * 遵循 SemVer 规范
 */

// 整体库版本号
export const libraryVersion = '1.0.0';

// 组件版本号
export const componentVersions = {
  Button: '1.0.0',
  Input: '1.0.0',
  Select: '1.0.0',
  Modal: '1.0.0',
  // 后续添加的组件版本号将在此处配置
};
